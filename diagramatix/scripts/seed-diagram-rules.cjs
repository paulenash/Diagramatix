// Seed default DiagramRules for all diagram types
const pg = require("pg");
// Honour DATABASE_URL when provided (so the same script seeds prod via
// `DATABASE_URL="<prod>" node scripts/seed-diagram-rules.cjs`); fall
// back to the local-dev default otherwise.
const pool = new pg.Pool({
  connectionString: process.env.DATABASE_URL ?? "postgres://postgres:postgres@localhost:5432/diagramatix",
  max: 1,
});

const rules = {
  "general": "## Group 1: General Layout\nG1.01: Diagrams should flow left-to-right as the primary direction.\nG1.02: Elements should be evenly spaced and well-organised.\nG1.03: Avoid overlapping elements and crossing connectors where possible.\nG1.04: Use clear, descriptive labels for all elements.\nG1.05: Labels should be action-oriented verb phrases for tasks (e.g. \"Check Order\", \"Approve Request\").\n\n## Group 2: General Naming\nG2.01: Element names should be concise but descriptive (3-5 words maximum).\nG2.02: Avoid abbreviations unless they are universally understood in the domain.\nG2.03: Use consistent naming conventions throughout the diagram.",
  "bpmn": "## Group 1: Pools & Lanes\nR1.01: Always create a main process White-Box Pool with the name of the entity carrying out the main process, or use the default name \"Company\".\nR1.02: Any references to external entities (e.g. Customer, Client, Government Department, Applicant, Registrant, Supplier) should be represented as Black-Box Pools at the top of the diagram. There may be more than one. These are non-System pools.\nR1.03: Any references to IT systems or commonly used business applications (e.g. Salesforce, XERO, SAP, SharePoint, databases, APIs) should be represented as Black-Box Pools below the main process Pool. These are System pools.\nR1.04: If Teams, Roles, or Departments are mentioned that can be interpreted as being within the Company carrying out the process, they should be represented as Lanes within the main process Pool, each Lane named after that Team, Role, or Department.\n\n## Group 2: Task Allocation & Types\nR2.01: Any decision, action, task, or process (sub-process) that is described as being done or carried out by a Role, Team, or Department MUST be placed in the Lane that is specifically named after that Role, Team, or Department. If no such Lane exists yet, create it. This applies to gateways (decisions) and sub-processes just as much as to ordinary tasks.\nR2.02: Tasks that involve sending to or receiving from a non-System external entity (e.g. Customer, Client) should use Message Flow connectors. The sending task should have taskType \"send\" and the receiving task should have taskType \"receive\".\nR2.03: Tasks that interact with System pools (IT systems like Salesforce, XERO, SAP) should use Message Flow connectors BUT the task should keep taskType \"user\" or \"service\" (NOT \"send\" or \"receive\"). Systems are accessed by users, not sent messages to.\n\n## Group 3: Flow Structure\nR3.01: Every process must start with exactly one Start Event on the left side, within the main process Pool.\nR3.02: Every process must end with one or more End Events on the right side, within the main process Pool.\nR3.03: CRITICAL: Any decision point that creates a diverging flow MUST always have a corresponding downstream Merge Gateway to reconnect ALL branches before any subsequent task.\nR3.04: Use Exclusive Gateways for if/else decisions with condition labels on the outgoing flows.\nR3.05: Use Parallel Gateways for concurrent activities that must all complete before proceeding.\nR3.06: Use Event-based Gateways (gatewayType=\"event-based\") when the process WAITS for one of several alternative EVENTS to occur and then follows a different path depending on which fires first — e.g. \"wait for Payment Received, Order Cancelled, or a Timeout, then handle each\". Model it EXACTLY like an Exclusive split/merge (R3.04): (a) both the splitting gateway (gatewayRole=\"decision\") and the re-joining gateway (gatewayRole=\"merge\") use gatewayType=\"event-based\" so both render with the event marker; (b) the FIRST element on each outgoing branch is that alternative's catching intermediate-event; (c) each event then flows through its OWN task(s)/subprocess(es) just like an exclusive branch; (d) only AFTER each branch's work is complete do all branches converge on the shared Event-based Merge Gateway. NEVER connect an event straight to the merge gateway — every event begins its own flow of tasks, and it is the LAST element of each branch that connects to the merge. Do NOT use event-based gateways for plain value-based if/else; those stay Exclusive (R3.04).\nR3.07: Sequence connectors leaving an edge-mounted Start Event MUST emit from its INNER side (the side facing into the host Expanded Subprocess). For the typical left-edge mount, this is the right-hand connection point.\nR3.08: Sequence connectors targeting an edge-mounted Start Event MUST attach at its OUTER side (the side facing AWAY from the host EP). Such connectors are only valid when the source element sits OUTSIDE the host EP — the boundary start represents an external trigger.\nR3.09: Sequence connectors targeting an edge-mounted End Event MUST attach at its INNER side AND originate from an element INSIDE the host EP. No element outside the EP may connect to a boundary end event.\nR3.10: An Expanded Subprocess MUST NEVER auto-connect to one of its own descendants. EP-to-child sequence connectors are disallowed regardless of the child's relative position inside the EP.\n\n## Group 4: Naming & Labels\nR4.01: Task names should be action-oriented verb phrases (e.g. \"Check Order\", \"Create Invoice\", \"Send Email\").\nR4.02: Gateway condition labels should be clear Yes/No or descriptive conditions.\nR4.03: Pool and Lane names should match the entity or role names mentioned in the process description.\n\n## Group 5: Layout Preferences\nR5.01: The process should flow left-to-right within the main Pool.\nR5.02: External entity (non-System) Pools should be positioned above the main Pool.\nR5.03: System/application Pools should be positioned below the main Pool.\nR5.04: Lanes should be ordered top-to-bottom in logical process flow order.\n\n## Group 6: Layout Sizing\nR6.01: Pool height must accommodate the vertical pool name text with comfortable spacing.\nR6.02: Lane width should be content width plus one task width of padding for later adjustment.\nR6.03: Message flows must attach at points that produce vertical connectors between pools.\nR6.04: When a message connector is created from a task to a non-System black-box pool, the task type is set to Send (source) or Receive (target). When connecting to a System black-box pool, the task type is set to User regardless of direction.\nR6.05: All process elements (tasks, events, gateways, subprocesses) must be FULLY contained within their Pool and Lane structure. The Pool and Lanes must grow to accommodate their content — including large Expanded Subprocesses and their boundary events.\nR6.06: Elements are divided between Lanes/Sublanes based on the performer (role or team) responsible for each element.\nR6.07: Any mention of an Event Expanded Subprocess sets its Usage to \"Event\" AND automatically places a Start Event inside it (not on the boundary). The Start Event's interruption type is chosen per R6.11: Non-Interrupting when inner tasks run in parallel with outer tasks, Interrupting when outer tasks are put on hold. When unclear, default to Non-Interrupting.\nR6.08: The nearest internal Task or Subprocess to an edge-mounted End Event is auto-connected to that End Event with a sequence connector.\nR6.09: The nearest internal Task or Subprocess to an edge-mounted Start Event is auto-connected from that Start Event with a sequence connector.\nR6.10: Event Expanded Subprocesses are always created INSIDE a containing Normal Expanded Subprocess (never at pool/lane level).\nR6.11: Event Expanded Subprocesses are smaller than Normal ones. They lay their children out in a single horizontal row (Start Event first, End Event last, any middle elements evenly spaced between), so the width grows with the child count — at least 4 x Task width, wider when there are more children — and they are 2 x Task height tall. Normal Expanded Subprocesses are sized to fit their child grid (with a small minimum so even a tiny one still reads as a container), rather than a fixed size. They contain either, a Non-Interrupting Start Event, if it is clear the tasks in this Event Expanded Subprocess are performed in parallel to the tasks in the outer Expanded Subprocess, OR an Interrupting Start Event if the outer tasks are on hold while the inner tasks are being performed. This Start Event should be near the left boundary and an End Event near the right boundary — both INSIDE the Event Expanded Subprocess (not boundary events).\nR6.12: NEVER create sequence connectors TO or FROM an Event Expanded Subprocess. Event subprocesses are triggered by events, not by sequence flow.\nR6.13: Every process must include a Start Event and at least one End Event at the main Process Pool level (outside any subprocess). These represent the overall process entry and exit.\nR6.14: A Gateway with one (or zero) incoming sequence connector and two or more outgoing sequence connectors must have gatewayType=\"none\" and gatewayRole=\"decision\" (unless an explicit marker such as parallel or inclusive was set).\nR6.15: A Gateway with two or more incoming sequence connectors and one (or zero) outgoing sequence connector must have gatewayType=\"none\" and gatewayRole=\"merge\" (unless an explicit marker was set).\nR6.16: Decision gateway wiring — the incoming sequence connector attaches to the left diamond point; outgoing connectors attach in AI order to top, then bottom, then right (for up to three outgoing branches).\nR6.17: Merge gateway wiring — the outgoing sequence connector attaches to the right diamond point; incoming connectors attach in AI order to top, then bottom, then left (for up to three incoming branches).\nR6.18: Event-based gateway wiring — Event-based gateways wire EXACTLY like the Exclusive decision/merge gateways (R6.16/R6.17); only the event marker and the absence of a label differ. The Event-based Decision Gateway attaches its incoming connector to the LEFT vertex and its outgoing connectors, ordered top-to-bottom by branch, to the TOP, RIGHT, then BOTTOM vertices (top and bottom only for exactly two branches). Each outgoing flow runs to that branch's catching intermediate-event — always entering the event on its LEFT connection point (never top/bottom), so the up/down branches route out of the gateway then turn into the event's left side — and the branch then continues through its own tasks/subprocesses. The matching Event-based Merge Gateway attaches its outgoing connector to the RIGHT vertex and its incoming connectors — one from the LAST element of each branch, NOT from the events — to the TOP, LEFT, then BOTTOM vertices. Branch order is preserved end-to-end: the branch leaving the decision's TOP enters the merge's TOP, middle→middle, bottom→bottom. Both gateways carry the event marker and stay unlabelled (no \"Test?\" question).\nR6.19: Pool height must accommodate the longest line of the (possibly multi-line) pool name at the current pool font size, plus a small vertical padding. When the pool name is split across lines via Shift-Enter, the pool height shrinks to fit the now-shorter longest line.\nR6.20: Pool header strip width grows when the pool name spans more than ~2 lines (or uses a larger font), and shrinks back toward the 36px default when fewer lines are needed. Children inside the pool are shifted horizontally by the same delta so they stay anchored to the pool body.\nR6.21: Lane height must be at least the longest line of the lane label at the current lane font size, plus padding. Lanes never auto-shrink — they only grow to fit. Growth propagates up to the parent lane (for sublanes) and to the containing pool.\nR6.22: Lane and sublane header widths are synchronised across all sibling lanes within the same parent — every sibling adopts the maximum needed width so the lane stack stays visually aligned. The shared width shrinks back to the 36px default when no sibling needs more.\nR6.23: Resizing a Pool, Lane, or Sublane is clamped to a minimum height equal to the larger of (the container's own label-driven minimum) or (the sum of every nested lane/sublane's minimum). Labels can never be cropped past the container boundary by a manual resize.\n\n## Group 7: Container Edit Behaviours (informational)\nR7.01: When a Lane or Sublane containing direct sublane children is deleted, those sublanes are PROMOTED to the deleted container's parent. They proportionally fill the deleted container's vertical slot, their headers re-align flush against the new parent's header strip (pool header for promoted-to-pool, lane header for promoted-to-lane), and any deeper grandchildren proportionally rescale to continue tiling each promoted parent. Sibling lanes at the deleted container's level are not affected.\nR7.02: When a Pool, Lane, or Sublane is resized (top or bottom edge dragged), the boundary delta is absorbed by the single adjacent lane on the dragged edge — other siblings keep their height. Inside that absorbing lane, every descendant sublane is then proportionally rescaled all the way down so each level continues to tile its parent's body.\nR7.03: When a Lane delete (manual or via the \"absorber\" rule) leaves only one Lane remaining inside a Pool, that lone Lane is automatically dissolved. If the dissolved lane has its own direct sublanes those promote into the Pool with their headers re-aligned (per R7.01); if it has none, the Pool reverts to a flat (no-lane) Pool. The dissolve loops until the Pool either has 0 or 2+ direct lanes.\nR7.05: [PROPOSED] Black-box Pool names that contain multiple words must have line breaks (Shift-Enter, i.e. embedded \\n characters) inserted on word boundaries so the LONGEST single line is as short as possible. This minimises the pool height on the generated diagram — the rotated header runs along the pool's height, so a shorter longest-line allows a shorter pool. Break only on natural word boundaries (e.g. \"Customer Support\\nService\", \"Finance\\nAudit\\nSystem\"); never split mid-word.\n\n## Group 8: Auto-Layout Placement\nR8.01: Decision gateway pairs — the decision gateway and its associated merge gateway are positioned vertically at the Y midpoint of the first following Task / Subprocess on each outgoing branch, irrespective of which lanes those branches enter. Both gateways share the same Y so the decision-merge pair reads as a horizontal divergence-and-reconvergence band across the spanned lanes.\nR8.02: Data Objects are auto-positioned relative to their associated element. A Data Object connected via an outwards-directed association from the Data Object to the element is an Input Data Object and is placed to the upper-left (preferred) or lower-left of the associated element. A Data Object connected via an inwards-directed association from the element to the Data Object is an Output Data Object and is placed to the upper-right (preferred) or lower-right. The Data Object's role property is set to \"input\" or \"output\" accordingly so the rendering matches the placement.\nR8.03: Data Stores are auto-positioned as near as possible to the elements they are connected to. When connected to a single element the Data Store sits centred above (preferred) or below that element. When connected to multiple elements the Data Store sits at the horizontal centroid of those elements and is offset above (preferred) or below the row so it stays clear of the horizontal sequence connectors flowing between them.\nR8.04: Loop-back (rework) sequence connectors — when a sequence connector runs from an element BACK to an element placed to its left or lower-left, it is routed UNDERNEATH the flow: it attaches to the BOTTOM of the source and to the BOTTOM of the target, then detours below the intervening elements. This keeps the implied loop clear of the forward flow and avoids connector crossings on the route.",
  "state-machine": "## Group 1: States\nS1.01: Every state machine must have exactly one Initial State (filled circle) and at least one Final State (bull's eye).\nS1.02: States should be named with nouns or adjective-noun phrases describing the condition (e.g. \"Idle\", \"Processing\", \"Awaiting Approval\").\nS1.03: Use Composite States to group related sub-states that share common transitions.\nS1.04: Use SubMachine states for complex sub-processes that warrant their own diagram.\nS1.05: Use Fork/Join bars for concurrent state transitions.\nS1.06: State names are Capitalised (first letter). Discovery enforces this automatically (e.g. a mined status value \"placed\" becomes \"Placed\") so a discovered state machine reads consistently and lines up with a conventionally-capitalised reference. Conformance matching is case-insensitive regardless.\nS1.07: When a state is INFERRED from an activity (a log with no explicit state/status column), the activity's leading verb is past-participled so the state reads as a condition, not a command — \"Ship\" becomes \"Shipped\", \"Cancel\" becomes \"Cancelled\", \"Pay\" becomes \"Paid\". The user can override any inferred state (e.g. a gerund like \"Closing\" for a transitional state).\n\n## Group 2: Transitions\nS2.01: Transitions should be labelled with the event or condition that triggers them.\nS2.02: Use formal transition labels where appropriate: event [guard] / actions.\nS2.03: Self-transitions are valid for states that loop on certain events.\nS2.04: Decision gateways should be used for complex branching logic with multiple conditions.\n\n## Group 3: Layout\nS3.01: [MODIFIED] Initial State should be on the top left, Final State(s) on the bottom right.\nS3.02: [MODIFIED] States should flow generally left-to-right showing progression through the states.\nS3.03: Composite States should be large enough to contain all sub-states clearly.\nS3.04: Transition connection point must not be shared and should be separated by at least 10px.\nS3.05: If two States have a Transition to and from each other then, the transition connectors must not cross each other.\nS3.06: Transition labels must not overlap and in particular, must be separated from one another vertically by at least 1/2 a label height.\nS3.07: [PROPOSED] When laying out States, alternate them above and below a central line so successive states zig-zag rather than sitting in one flat row. This angles the transition connectors so they are more clearly differentiated from one another.",
  "value-chain": "## Group 1: Process Layout\nV1.01: Processes should be arranged left-to-right representing the value chain flow.\nV1.02: Related processes should overlap by 10px to create the interlocking chain appearance.\nV1.03: Each process should have a clear, concise name.\nV1.04: Always use Collapsed Process elements for generated Value Chain processes.\n\n## Group 2: Value Chains\nV2.01: Use Value Chain containers to visually group related processes.\nV2.02: Value Chain containers should have a descriptive name at the top.\nV2.03: Nested Value Chain containers automatically lighten in shade for visual hierarchy.\nV2.04: The enclosing Value Chain must allow enough gap at the bottom to comfortably enclose the processes and their descriptions.\n\n## Group 3: Descriptions\nV3.01: Each process should have a description that summarises the process scope.\nV3.02: Descriptions are shown by default below each process.\nV3.03: Descriptions should auto-wrap within the process width.\n\n## Group 4: Colour Themes\nV4.01: Use the Garden colouring theme by default for generated processes.\nV4.02: Use colour themes to distinguish different process areas or departments.\nV4.03: Apply themes by selecting multiple processes and right-clicking.",
  "domain": "## Group 1: Entities\nD1.01: Each table or entity should be represented as a UML Class with the <<entity>> or <<table>> stereotype.\nD1.02: All attributes should include their data type.\nD1.03: Primary keys should be marked with {PK}.\nD1.04: Foreign keys should be marked with {FK} and reference the target table.\nD1.05: NOT NULL constraints should be indicated with [1] multiplicity.\n\n## Group 2: Relationships\nD2.01: Use UML Association connectors for foreign key relationships.\nD2.02: Show multiplicities on both ends of associations (e.g. 1..*, 0..1, 1..1).\nD2.03: Use composition for strong ownership relationships.\nD2.04: Use aggregation for weak containment relationships.\n\n## Group 3: Enumerations\nD3.01: Lookup/reference tables should be represented as UML Enumerations with <<enumeration>> stereotype.\nD3.02: All valid values should be listed in the enumeration body.\n\n## Group 4: Layout\nD4.01: Entity tables should be arranged in a grid layout.\nD4.02: Related entities should be positioned near each other.\nD4.03: Enumeration tables should be grouped separately from entity tables.\n\n## Group 5: Connector Routing\nD5.01: Where several connectors attach to the same side of an element, spread their attachment points evenly along that side so none coincide — N connectors divide the side into N+1 sections (offsets 1/(N+1) … N/(N+1)), kept clear of the corners so multiplicity and role labels have room; order them along the side by their opposite endpoint so siblings on a side do not cross. Applied when a diagram is generated or a connector is added or removed; a user's manually-positioned endpoint is preserved.\nD5.02: No two connectors share the same attachment point and no connector segment lies directly on top of another. Where two connectors would cross, remove the crossing where possible by re-ordering their attachment points along a shared side (or moving an endpoint to a less-congested side) — never by reversing a connector's direction, which would flip its arrowhead or diamond and change its meaning. Crossings that cannot be removed this way are permitted.",
  "context": "## Group 1: Elements\nC1.01: The central system or process should be represented as a large ellipse or rectangle in the centre.\nC1.02: External entities should be arranged around the central system.\nC1.03: Data flows should be labelled with the data or information being exchanged.\n\n## Group 2: Layout\nC2.01: External entities should be evenly spaced around the central system.\nC2.02: Flow lines should not cross where possible.\nC2.03: Use bi-directional flows where data moves in both directions.",
  "process-context": "## Group 1: Elements\nP1.01: Use Cases should represent the main processes or functions.\nP1.02: If a team or department is mentioned, use the Team element, NOT Actor.\nP1.03: If an IT system is mentioned that a process interacts with, use the System element with the system name.\nP1.04: If a scheduler, timer, or automated trigger is mentioned, use the Hourglass (Auto Scheduler) element.\nP1.05: The container name MUST always include the words \"Process Group\" (e.g. \"HR Process Group\"). This is a Process Context diagram, NOT a Use Case diagram.\nP1.06: Process numbering format is P-XX-NN where XX is a 2-3 character code (e.g. P-HR-01, P-HR-02, P-FI-01).\n\n## Group 2: Layout\nP2.01: Processes should be laid out 1 per horizontal row, zigzagging left and right down the page to maximise clear connection availability.\nP2.02: Actors should be placed between the processes they are connected to, to minimise association crossings.\nP2.03: Actors and Teams should be shown to the left for processes on the left, and to the right for processes on the right.\nP2.04: System actors should be placed to the right of the process group boundary, in empty space where possible.\nP2.05: Hourglass (Auto Scheduler) actors should be placed to the right of the process group boundary.\nP2.06: The process group boundary should be portrait in shape with enough room for processes with gaps to the vertical boundaries.\nP2.07: Actor placement should try to eliminate or at least minimise association line crossings.\n\n## Group 3: Connectors\nP3.01: Hourglass actor connectors must be open-directed (open arrowhead) pointing from the hourglass toward the process it triggers.\nP3.02: All other actor/team/system associations are non-directed (no arrows).",
  "archimate": "## Group 1: Elements\nA1.01: Use only Business and Application layer elements: business-actor, business-role, business-interface, business-collaboration, business-service, business-process, business-function, business-interaction, business-event, product, application-component, application-service, application-interface, application-collaboration, data-object.\nA1.02: Represent each end-to-end value-chain stage as a business-process and put its identifier in the label (e.g. \"V01.01 Receive Order\") so it can be linked to its detailed BPMN diagram.\nA1.03: Represent customer-facing outcomes as business-service elements (e.g. \"Product Ordering Service\").\nA1.04: Represent IT systems as application-component elements and stored data as data-object elements.\nA1.05: Represent the external party as a business-actor; represent internal performers as business-role where useful.\n\n## Group 2: Relationships\nA2.01: A business-process REALISES the business-service it delivers (realisation, process to service).\nA2.02: An application-component SERVES the business-process it supports (serving, component to process).\nA2.03: An actor is ASSIGNED to a role; a role is ASSIGNED to the process it performs (assignment).\nA2.04: An actor ACCESSES a business or application interface; the interface SERVES the service behind it.\nA2.05: A process ACCESSES a data-object it reads or writes (access).\nA2.06: Use triggering or flow for process-to-process sequence; composition or aggregation for whole-part; association only when nothing more specific fits.\n\n## Group 3: Naming\nA3.01: Element names should be concise noun phrases; service names end in \"Service\".\nA3.02: Keep value-chain process identifiers consistent with the source BPMN process names.\n\n## Group 4: Layout & Positioning\nA4.01: Lay the diagram out in horizontal bands by layer: business active-structure (actors, roles, interfaces) on top, then business services, then business behaviour (processes, functions, events), then application services and interfaces, then application components and data objects at the bottom.\nA4.02: Within a band, order elements left-to-right to follow the customer journey.\nA4.03: Do not output coordinates; the layout engine positions elements and routes relationships.\nA4.04: [PROPOSED] Never connect relationship connectors to the same connection point — always offset by 5px.\nA4.05: [PROPOSED] Minimise connector crossings by placing Application layer elements underneath, but as near as possible to, their connected Business layer elements.\nA4.06: [PROPOSED] If not generated this way, ensure elements are sized so their names fit inside the element boundary, and break longer names after any numbering, every 2 words, onto separate lines.\nA4.07: [PROPOSED] Connectors must always avoid routing through other elements; route around any intervening element leaving a 10px gap on all sides.\nA4.08: [PROPOSED] Element numbering must always be shown on its own line at the top of a multi-line name. Recognise a leading code to split off using these patterns (A = an alphabetic character A-Z or a-z, n = a digit 0-9, . = a separator such as . , : ; -): An.n, An.nn, Ann.n, Ann.nn, AAn.n, AAn.nn, AAnn.n, AAnn.nn, AAAn.n, AAAn.nn, AAAnn.n, AAAnn.nn.",
  "flowchart": "## Group 1: Symbols & When To Use Them\nF1.01: Use a Terminator (stadium) for the single Start and for each End/Stop of the flow.\nF1.02: Use a Process (rectangle) for an action, task, operation, or calculation — this is the default step.\nF1.03: Use a Decision (diamond) for any branching question or test; it must have two or more labelled outgoing branches.\nF1.04: Use Input/Output (parallelogram) when data enters or leaves the process.\nF1.05: Use a Document for a single produced/consumed document, and Multiple Documents for a pack of them.\nF1.06: Use a Predefined Process (double-bar rectangle) for a named sub-process defined elsewhere.\nF1.07: Use Preparation (hexagon) for setup/initialisation steps, Manual Input (sloped-top) for data keyed by a person, and Manual Operation (inverted trapezoid) for work performed by a person.\nF1.08: Use Display for information shown on a screen, Delay for a deliberate wait, and Database (cylinder) for a structured electronic data store.\nF1.09: Use On-page Connector (circle) to continue the flow elsewhere on the same page and Off-page Connector (pentagon) to continue on another page.\nF1.10: Use a Merge (down-triangle) only when several branches explicitly recombine into one flow.\n\n## Group 2: Flow & Branch Semantics\nF2.01: The flow has exactly one Start terminator and finishes at one or more End terminators.\nF2.02: Model the main sequence as a single chain of Process steps from Start toward End.\nF2.03: Every Decision is phrased as a question ending in \"?\" and each outgoing connection carries a short branch label (e.g. \"Yes\"/\"No\").\nF2.04: Branches either rejoin at a later step or at an explicit Merge before continuing; never leave a branch dangling without reaching an End.\nF2.05: Connections are Flowlines (directed arrows) from source to target.\n\n## Group 3: Naming & Labels\nF3.01: Process names are action-oriented verb phrases (e.g. \"Validate Order\", \"Send Invoice\").\nF3.02: Decision labels are clear yes/no or true/false questions.\nF3.03: Keep labels concise (3-5 words) and consistent in tense and voice.\n\n## Group 4: Layout & Positioning\nF4.01: The flow is laid out top-to-bottom; the Start terminator is at the top.\nF4.02: A Decision's branches are placed side-by-side in the next row and re-converge lower down.\nF4.03: Elements are evenly spaced and Flowlines routed to minimise crossings.\nF4.04: Do not output coordinates; the layout engine positions elements and routes the flowlines.",
};

async function seed() {
  for (const [category, text] of Object.entries(rules)) {
    // UPSERT on id so re-running the seed propagates updated rule text
    // to the existing default rows. The fixed id is `default-{category}`.
    await pool.query(
      `INSERT INTO "DiagramRules" (id, category, rules, "isDefault", "createdAt", "updatedAt")
       VALUES ($1, $2, $3, true, NOW(), NOW())
       ON CONFLICT (id) DO UPDATE
         SET rules = EXCLUDED.rules,
             "updatedAt" = NOW()`,
      [`default-${category}`, category, text]
    );
    console.log(`Seeded: ${category} (${text.split("\n").filter(l => l.match(/^[A-Z]\d+(?:\.\d+)*:/)).length} rules)`);
  }
  await pool.end();
  console.log("Done!");
}

seed().catch(e => { console.error(e); process.exit(1); });
