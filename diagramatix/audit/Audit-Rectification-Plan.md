# Diagramatix — Audit Rectification Plan

| | |
|---|---|
| **Created** | 2026-08-14 |
| **Baseline audit** | [`Source-Code-Audit.md`](./Source-Code-Audit.md) — staged multi-agent review, 2026-06-13 → 2026-06-26, ~103 findings, 36 fixed |
| **This document** | The **live worklist**. Every still-open finding from the baseline audit, re-verified against `HEAD`, plus new findings from a fresh sweep of surface added since the baseline (simulator, mining connectors, screencast, SOP, SharePoint, feature-availability, gateway/diagram work). Fixed items are **not** repeated here — they live in the baseline doc. |
| **Progress log** | **2026-08-14** — batch 1 (`64036360`): **SEC-01**, **SEC-20**, **SIM-01** (SIM-01 re-scoped on implementation — see its row). batch 2 (`31a03d67`): the rest of Wave A — SEC-03, SEC-13, SEC-14, SEC-21. batch 3 (`4d9b6fef`): all of Wave B — CFG-02/03/04/05/06/07 + MINE-01. **2026-08-15** — batch 4 (`629d2d26`): Wave C part 1 — DATA-21, DATA-09, DATA-14, DATA-19. batch 5 (`5855f19f`): Wave C part 2 — DATA-18, DATA-20, DATA-29, UI-03. batch 6 (`0fd515c2`): DATA-32 (new — mandatory optimistic-concurrency on data saves; found reviewing co-authoring). Wave C complete except DATA-15 (parked, Stripe test-mode). batch 7 (`00f12419`): Wave D part 1 — SEC-08 (user-search scoping), SEC-09 (serverError sanitising helper across 20 catch-all 500s). batch 8 (`e59e2cf9`): Wave D part 2 — DATA-08 (parentDiagramIds remap), DATA-10 (mail-header injection), DATA-30 (restore count + malformed-payload 400). **batch 9 (`2e44bdaf`): Wave D part 3 (engine) — ENG-05 (REMOVE_SPACE corners), ENG-07 (NaN offset guard), ENG-14 (label-edit redo). ENG-06 + ENG-08 deferred (need reproduction).** batch 10 (`34122800`): Wave D importers — IO-02/05/06/07/08/09/10 fixed, IO-03 closed N/A. **Wave E begins — batch 11: ENG-12 (routing ancestor Map), ENG-18 (memoised container-depth sort), IO-04 (BPMN import id→element index) — behaviour-preserving O(n²)→O(n) refactors covered by the existing routing/container/import suites.** **batch 12 (2026-08-16, `a8b16ab5`): ENG-09 (lane-swap z-order), ENG-10 (swapLane history cap), ENG-13 (waypoint aliasing), ENG-15 (correction-pass redo invalidation), ENG-17 (data-object connector recompute batched), ENG-19 (vswimlane descendant index), CFG-08 (drop dead PGlite direct deps); CFG-10 found already-mitigated (`?? 3600`). ENG-11 + ENG-16 + CANVAS-07/08/09 DEFERRED (interactive/visual/React-lifecycle — desk session).** **batch 13 (2026-08-16, `61627d49`): SEC-16 (reset token SHA-256 hashed at rest), SEC-17 (impersonation identity+mode cookies HMAC-signed). ⚠ deploy note: in-flight plaintext reset tokens + existing impersonation cookies invalidate on deploy (both re-triggerable).** |
| **How to use** | Work an item, set its **Status** to `In progress` → `Fixed` (with the commit), or `Won't fix` (with a reason). Keep the baseline doc as the historical record; keep this doc as the burn-down. |

**Status values:** `Not fixed` · `In progress` · `Fixed (<commit>)` · `Needs confirmation` (finding plausible but not yet reproduced) · `Won't fix (<reason>)`.

**Wave tag:** each Recommended fix is prefixed with its execution wave — **[Wave A]**…**[Wave F]** — matching the [Priority waves](#priority-waves) section below. A + B are complete.

**Severity:** Critical = exploitable hole / data loss · High = a bug users hit, or a security weakness needing circumstance · Medium = latent bug / race / robustness gap · Low = quality / performance / minor hardening.

---

## Re-verification summary (2026-08-14)

- **All baseline "Open" findings were re-checked against current code.** None of the open items have been silently fixed since 26 June; a few moved file/line but the defect stands. They are carried forward below with their original IDs.
- **Two findings are newly raised** from the post-baseline surface: **SIM-01** (unbounded simulation run — see its row; on implementation the "view-gated" half was found to be wrong, so it's Medium not High) and **MINE-01** (SSRF via operator-supplied poll URL). The large body of simulator/gateway work added this session was written test-first and is not a source of new correctness findings; the two items here are a resource-exhaustion gap and an egress gap, not logic bugs.
- **Scope note:** the baseline explicitly excluded the **test suite** and `app/generated/`. That exclusion still stands — a test-suite review remains a separate, unstarted piece of work (tracked as **TEST-01** below so it isn't lost).
- **Counts carried forward:** 12 Security · 12 Data-integrity · 15 Engine · 8 Canvas · 1 UI · 9 Import/Export · 10 Config + 2 new + 1 meta = **69 open items** at creation; **14 fixed 2026-08-14** (Wave A: SEC-01/03/13/14/20/21, SIM-01; Wave B: CFG-02/03/04/05/06/07, MINE-01) then **9 fixed 2026-08-15** (Wave C: DATA-21/09/14/19/18/20/29 + UI-03, plus the newly-raised **DATA-32**) → **47 open.** Waves A + B cleared; **Wave C complete except DATA-15** (parked for a dedicated Stripe-test-mode pass). *(DATA-32 was found + fixed same-day while reviewing the co-authoring concurrency model, so it doesn't change the open count.)* **Wave D then began: batch 7 (SEC-08/09) + batch 8 (DATA-08/10/30) + batch 9 (ENG-05/07/14) + batch 10 importers (IO-02/05/06/07/08/09/10 fixed, IO-03 closed N/A) → 31 open.** Wave E began: batch 11 (ENG-12/18 + IO-04 — id-index perf refactors) → 28 open; batch 12 (green-bucket hygiene — ENG-09/10/13/15/17/19 + CFG-08 fixed, CFG-10 already-mitigated) → 20 open; batch 13 (SEC-16 reset-token-hash + SEC-17 signed impersonation cookies) → **18 open.**
- **New observation while doing CFG-05/06:** the `/test/*` scratch pages (`/test/db`, `/test/session`, `/test/api`) build into production and are **not** behind the matcher. They look like dev-only debug pages that may expose DB/session state unauthenticated — logged here as **CFG-11** (Low) for a follow-up; not fixed in this batch.

---

## Priority waves

The order below is the recommended execution sequence. It supersedes the baseline's original wave numbering (which was written before the completed fixes).

- **Wave A — Security you can fix in a day each:** ✅ **COMPLETE 2026-08-14** — ~~SEC-01~~, ~~SEC-03~~, ~~SEC-13~~, ~~SEC-14~~, ~~SEC-20~~, ~~SEC-21~~, ~~SIM-01~~.
- **Wave B — Config & platform hardening (broad blast radius, low code):** ✅ **COMPLETE 2026-08-14** — ~~CFG-02~~, ~~CFG-03~~, ~~CFG-04~~, ~~CFG-05~~, ~~CFG-06~~, ~~CFG-07~~, ~~MINE-01~~.
- **Wave C — Data-integrity / data-loss:** ✅ **COMPLETE except DATA-15 (parked)** — ~~DATA-09~~, ~~DATA-14~~, **DATA-15** *(parked, Stripe test-mode)*, ~~DATA-18~~, ~~DATA-19~~, ~~DATA-20~~, ~~DATA-21~~, ~~DATA-29~~, ~~UI-03~~.
- **Wave D — Correctness & robustness:** 🔶 IN PROGRESS — ~~SEC-08~~, ~~SEC-09~~ *(batch 7, 2026-08-15)*; ~~DATA-08~~, ~~DATA-10~~, ~~DATA-30~~ *(batch 8)*, ~~ENG-05~~, ~~ENG-07~~, ~~ENG-14~~ *(batch 9)*; **importers batch 10 (2026-08-15, `34122800`):** ~~IO-02~~, ~~IO-05~~, ~~IO-06~~, ~~IO-07~~, ~~IO-08~~, ~~IO-09~~, ~~IO-10~~ + **IO-03 closed N/A** (importer is an iterative walker — no recursion depth, no stack risk); **ENG-06 + ENG-08 DEFERRED** (subtle — need interactive/visual reproduction); remaining Import/Export: **IO-04** (O(n²) parse — a perf item; ✅ now fixed in Wave E batch 11). *(IO-01 was already fixed in the baseline's urgent batch, 2026-06-26 — my earlier note listing it as open was wrong.)*
- **Wave E — Performance (large-diagram pan/zoom):** 🔶 IN PROGRESS — **batch 11 (2026-08-15, `fe780b0a`):** ~~ENG-12~~ (routing ancestor index), ~~ENG-18~~ (memoised container-depth sort), ~~IO-04~~ (BPMN import id→element index). **batch 12 (2026-08-16, `a8b16ab5`):** ~~ENG-17~~ (data-object connector recompute batched), ~~ENG-19~~ (vswimlane descendant index). **Remaining (desk session — visual verify): CANVAS-02/03/04 + CANVAS-05 (paused) + CANVAS-06.**
- **Wave F — Low / cleanup:** ~~ENG-09~~, ~~ENG-10~~, ~~ENG-13~~, ~~ENG-15~~ *(batch 12, 2026-08-16)*, ~~CFG-08~~ *(batch 12)*, CFG-10 *(already mitigated)*; **ENG-11 + ENG-16 + CANVAS-07/08/09 DEFERRED** (interactive/visual/React-lifecycle — desk session). ~~SEC-16~~, ~~SEC-17~~ *(batch 13, 2026-08-16)*. Remaining: **SEC-18** *(product decision — implement OrgAdmin impersonation or remove the misleading UI)*, **DATA-24** *(architectural — dual pool can't share a txn)*, ENG-16, CANVAS-07…09, **CFG-01** *(ops — rotate `.env`)*, **CFG-09** *(risky — next-auth beta upgrade)*, plus **TEST-01** *(review effort — audit the suite)*.

---

## Security & access control

| Code | Sev | Nature of the issue | Risk if not fixed | Recommended fix | Status |
|---|---|---|---|---|---|
| SEC-01 | High | A user holding only a **VIEW** share of a project can delete it: the delete route's OrgAdmin probe (`requireRole` in [`app/api/projects/[id]/route.ts`](../app/api/projects/[id]/route.ts)) resolves against the caller's *current* org, not the *project's* org, so a shared-in user who is Owner/Admin of their **own** org passes the `isOrgAdmin` gate for someone else's project. | A collaborator (or anyone in a cross-org share) can destroy a project they were only meant to view. Data loss + trust breach. | **[Wave A]** Compute `isOrgAdmin` against the **project's** `orgId` (from `requireProjectAccess`), not the caller's current org. Pass that org into the role probe. | **Fixed 2026-08-14** — the probe now calls `requireOrgAdminFor(session, cookies, projectOrgId)` instead of `requireRole` (which used the caller's current org). Test in `tests/projects/delete-authorization.test.ts` pins that an Owner of org A is denied OrgAdmin for org B. |
| SEC-03 | High | The OrgAdmin scoped backup serialises full `User` rows (`prisma.user.findMany` in [`org-backup.ts`](../app/lib/org-backup.ts)) — password hash, reset token, reset expiry, Stripe customer/subscription ids — into a file any OrgAdmin can download. | An OrgAdmin exfiltrates every member's credential hash + password-reset token (offline cracking, account takeover). Privilege escalation within a tenant. | **[Wave A]** Select only the columns a restore needs; drop `password`, `resetToken`, `resetTokenExpiry`, Stripe ids from the exported user projection. Restore already re-parents by email, so hashes aren't needed. | **Fixed 2026-08-14** — export nulls those five columns on every User row; the additive-restore CREATE path also strips them (defends against pre-fix files, and avoids a stale unique Stripe-id collision). A created user gets password `""` (schema default) → login disabled until reset. Left the SuperAdmin **full** backup untouched, which needs hashes for disaster-recovery login and is already inside that trust boundary. 2 tests in `tests/backup/org-backup-redaction.test.ts`. |
| SEC-08 | Medium | User search ([`app/api/users/search/route.ts`](../app/api/users/search/route.ts)) matches `name`/`email` across **all** tenants with no org scoping, returning email addresses. | Cross-tenant directory harvesting: any authenticated user enumerates every user's email. Privacy / GDPR exposure. | **[Wave D]** Scope the query to users who share an org (or an existing collaboration surface) with the caller; or return only within-org matches. | **Fixed 2026-08-15** — scoping extracted to `app/lib/userSearch.ts`: shared-org colleagues match on a partial name/email fragment; anyone else ONLY by an EXACT email (so an external invite-by-known-address still works, but `@gmail.com` can't harvest a directory). 4 tests. |
| SEC-09 | Medium | Raw Postgres / internal error text is returned to clients in several catch-alls. | Leaks schema, table names, and stack internals — reconnaissance that eases further attacks. | **[Wave D]** Return a generic message + a correlation id to the client; log the detail server-side only. Centralise via a small error responder. | **Fixed 2026-08-15** — new `serverError(err, ctx)` (`app/lib/apiError.ts`) logs full detail server-side under a short correlation `ref` and returns only a generic message + ref. Applied to ALL 20 catch-all 500s that echoed a raw `{ error: message }` (backup/restore, diagram save/publish/archive, bundles, admin/database, templates, sim run, etc.). Residual: a few 500s still include `err.message` behind a friendly fallback (e.g. Visio export "Export failed") — low risk, follow-up. 3 tests. |
| SEC-13 | Medium | Archived-diagram **delete/restore** routes never call `isReadOnlyImpersonation`. | A SuperAdmin impersonating a user in **read-only** mode can still destroy/restore that user's archived diagrams — the read-only promise is violated. | **[Wave A]** Add the `isReadOnlyImpersonation` guard (403) to both routes, matching every other mutating route. | **Fixed 2026-08-14** — guard added to BOTH instances: the user-facing [`diagrams/deleted/route.ts`](../app/api/diagrams/deleted/route.ts) POST(restore)+DELETE(purge) (which resolve `getEffectiveUserId`, so they act on the *impersonated* user's diagrams — the directly-exploitable case), and the SuperAdmin [`admin/archive/route.ts`](../app/api/admin/archive/route.ts) POST+DELETE (the system-wide archive holds every user's diagrams). |
| SEC-14 | Medium | `scan-links` **POST** mutates diagram JSON with no read-only-impersonation guard ([`app/api/projects/[id]/scan-links/route.ts`](../app/api/projects/[id]/scan-links/route.ts) — gated at `edit` but not impersonation). | Same as SEC-13: a read-only impersonating admin can rewrite a user's diagram. | **[Wave A]** Add `isReadOnlyImpersonation` → 403 to the POST handler. | **Fixed 2026-08-14** — guard added to the POST handler (the GET/preview stays open). |
| SEC-20 | High | The **archive** (soft-delete) route [`app/api/diagrams/[id]/archive/route.ts`](../app/api/diagrams/[id]/archive/route.ts) checks `requireDiagramAccess(owner)` but **not** `isReadOnlyImpersonation`. | A read-only impersonating SuperAdmin can soft-delete the viewed user's diagram — the most-used "reach in and look" path can still mutate. | **[Wave A]** Add the `isReadOnlyImpersonation` → 403 guard at the top of the handler. | **Fixed 2026-08-14** — `isReadOnlyImpersonation` guard added (wrapped in try/catch for the `cookies()`-throws case), matching every other mutating route. |
| SEC-21 | Low | Prompt routes scope the **org** to the impersonated user but perform key writes against the **superuser's own** id. | Impersonated prompt edits land on the wrong user; subtle data mis-attribution during support sessions. | **[Wave A]** Resolve the effective user id (`getEffectiveUserId`) consistently for both the org scope and the write target. | **Fixed 2026-08-14** — [`prompts/route.ts`](../app/api/prompts/route.ts) + [`prompts/[id]/route.ts`](../app/api/prompts/[id]/route.ts) now resolve `getEffectiveUserId` for every owner filter and the create/update target. Because the writes now genuinely target the impersonated user, the mutating handlers (POST/PUT/DELETE) also gained the `isReadOnlyImpersonation` guard so the corrected attribution doesn't open a fresh SEC-13-class hole. |
| SEC-16 | Low | Password-reset token stored in plaintext at rest (`User.resetToken`, [`schema.prisma`](../prisma/schema.prisma)). | A read-only DB leak (or backup — see SEC-03) hands out live reset tokens → account takeover without cracking. | **[Wave F]** Store a SHA-256 hash of the token; compare hashes on redemption. One-line change either side + a short expiry (already present). | ✅ Fixed 2026-08-16 — reset token stored as SHA-256 hash; raw token only in the emailed link (`hashResetToken`) |
| SEC-17 | Low | Impersonation identity/mode cookies are `httpOnly` + `secure` but **unsigned** — their value is the target `userId` in the clear. | The cookies can't be read by JS, but they aren't integrity-protected; combined with any set-cookie surface, tampering is cheaper than it should be. Defence-in-depth gap. | **[Wave F]** Sign the cookie value (HMAC with `AUTH_SECRET`) or store a server-side session token instead of the raw id. | ✅ Fixed 2026-08-16 — impersonation identity+mode cookies HMAC-signed (`signedValue.ts`); tampered/unsigned → treated as absent |
| SEC-18 | Low | OrgAdmin "impersonation" is a server-side no-op — the UI implies a capability the backend doesn't honour. | Misleading: an OrgAdmin believes they are acting as a user but writes land as themselves. Confusion, mis-attributed changes. | **[Wave F]** Either implement OrgAdmin impersonation properly (scoped, audited) or remove the UI affordance. | Not fixed |
| SEC-19b | — | *(SEC-19 Deepgram key leak is **Fixed (Wave 1)** in the baseline — listed here only so the id isn't reused.)* | — | **[Wave A]** — | Fixed (Wave 1) |

## Simulation & connectors (new surface)

| Code | Sev | Nature of the issue | Risk if not fixed | Recommended fix | Status |
|---|---|---|---|---|---|
| SIM-01 | ~~High~~ **Medium** | The scenario **run** route [`…/scenarios/[scenarioId]/run/route.ts`](../app/api/projects/[id]/simulation/studies/[studyId]/scenarios/[scenarioId]/run/route.ts) spreads the persisted `runConfig` (horizon, replications) into the engine **unclamped**; `runMonteCarlo` loops synchronously in the request. **Correction on implementation:** the POST route is already gated at **`edit`** and already carries the `isReadOnlyImpersonation` guard — the original "view-gated" claim was a mis-read of the GET (run-history) handler's `view` check. So the real gap is only the missing clamp, and it requires **edit** access → severity dropped High→Medium. | Denial of service: an editor (or a malformed imported config) can pin a server core with an absurd `horizon × replications`. Bounded now to edit-holders, still unbounded compute. | **[Wave A]** Clamp `horizon`, `replications`, and their product before the engine sees the config. | **Fixed 2026-08-14** — new `clampRunConfig` + `RUN_LIMITS` in [`runner.ts`](../app/lib/simulation/runner.ts) (horizon ≤ 100k, replications ≤ 100, product ≤ 5M — over-cap shrinks replications, never the horizon, since a short horizon changes the model's meaning; warm-up kept < horizon; NaN/negative coerced). Applied at the run route. 8 tests in `tests/simulation/clamp-run-config.test.ts`. Gating was already correct — no change needed there. |
| MINE-01 | Medium | Live-source polling ([`app/lib/mining/pull.ts`](../app/lib/mining/pull.ts)) `fetch()`es a URL stored on the `MiningSource` with no host allow-list or private-range block. The URL is set by a project editor via the sources API. | SSRF: an editor can point a source at `http://169.254.169.254/…` or an internal service and have the server fetch it, exfiltrating cloud metadata / internal responses into the mining buffer. | **[Wave B]** Validate the configured URL at save time and re-validate at fetch time: require `https`, resolve the host and reject private/link-local/loopback ranges, and (ideally) restrict to an allow-list of connector hosts. | **Fixed 2026-08-14** — new [`mining/blobUrl.ts`](../app/lib/mining/blobUrl.ts) `assertSafeBlobUrl` requires `https` + an **Azure Blob Storage host suffix** (public + sovereign clouds), rejecting IP literals and lookalike suffixes. Chosen over a private-IP block because an attacker can't point `*.blob.core.windows.net` at an internal address → no DNS-rebinding TOCTOU. Enforced at the poller AND at save/reconfigure (create + PATCH routes). 12 tests. |

## Data integrity & server libraries

| Code | Sev | Nature of the issue | Risk if not fixed | Recommended fix | Status |
|---|---|---|---|---|---|
| DATA-08 | High | Subprocess-link remap on restore only walks `properties.linkedDiagramId`; other embedded diagram references (variant roots, etc.) aren't remapped. | A restored project's cross-diagram links point at old ids → broken drill-down / roll-up, silent in the UI. | **[Wave D]** Extend the remap to every field that can hold a diagram id (variant roots, any `*DiagramId`); drive it from one shared id-map pass. | **Fixed 2026-08-15** — restore now also remaps top-level `data.parentDiagramIds` (cross-diagram back-refs), not only element `linkedDiagramId`. Round-trip test. |
| DATA-09 | High | Bundle-invite promotion wraps its work in a catch-all that **deletes the pending row on any error** ([`bundleInvites.ts`](../app/lib/bundleInvites.ts)). | A transient failure (e.g. mail send) permanently discards a pending invite — the recipient silently never gets access. | **[Wave C]** Only delete the pending row on a *successful* promotion; on error, leave it for retry and surface the failure. | **Fixed 2026-08-15** — the catch now deletes the pending row ONLY on a Prisma P2002 (already a member = redundant invite); any other error keeps it for retry. The best-effort notification moved out of the transaction so a notify failure can't be mistaken for a promote failure. Tested incl. a fault-injected transient failure that then retries green. |
| DATA-10 | High | Invite-email Subject is built from unescaped inviter/bundle names → header-injection surface. | A crafted org/bundle name could inject mail headers (spoofed recipients, CC). | **[Wave D]** Strip CR/LF from any user-controlled value interpolated into a mail header; prefer a mail library that encodes headers. | **Fixed 2026-08-15** — new `mailHeader()` collapses CR/LF/tabs to a space (the header-injection vector) on every user value put in a Subject (bundle invite + support). 2 tests. |
| DATA-14 | High | Org backup pulls DiagramRules with `where: { userId: { in: memberUserIds } }` ([`org-backup.ts:92`](../app/lib/org-backup.ts)), so **org/admin-default** rules (`userId` null) are never captured. *(Confirmed 2026-08-14.)* | An org restore silently loses the org's default rule set → AI generation reverts to platform defaults. | **[Wave C]** Include rules where `orgId` matches (and `userId` null) in the org backup projection. | **Fixed 2026-08-15** — three parts: capture now `OR orgId` (picks up org defaults, userId null); `scopePayloadToOrg` keeps org-scoped rules; and the additive restore now RECREATES them (it previously captured but never re-inserted rules at all — so projection alone would not have fixed the stated risk). Owner remapped to the live user, org-scoped rules re-homed onto the target org, natural-key upsert (DATA-23 pattern). 2 tests. |
| DATA-15 | High | `checkLimit` / `recordUsage` are a read-then-write TOCTOU ([`subscription-route.ts`](../app/lib/subscription-route.ts)); concurrent requests both pass the check before either records. | Users exceed hard subscription caps under concurrency (free AI actions, imports) — revenue leak + resource abuse. | **[Wave C]** Atomic reserve-then-act-then-refund: a single conditional UPDATE that increments only if under cap, refunded on failure. Deferred in the baseline as a dedicated tested pass. | Not fixed |
| DATA-18 | Medium | `restoreDiagram` trusts the archived `userId` and never re-validates current org membership. | A user removed from an org can have a diagram restored under their id back into that org → stale cross-tenant data. | **[Wave C]** Re-validate that the target user is still a member of the diagram's org before restoring; else re-home or refuse. | **Fixed 2026-08-15** — `restoreDiagram` now checks `orgMember` for the original owner in the diagram's org and REFUSES with a clear error if they've left, rather than silently re-homing a diagram under a non-member. 2 tests (refuse-when-left; restore-when-member). |
| DATA-19 | Medium | Per-user backup captures cross-org prompts, but restore dedups them into a single org → prompts from other orgs are lost. | Silent data loss on restore for any user who is a member of multiple orgs. | **[Wave C]** Key the restore dedup by (org, name) not name alone; recreate per-org copies. | **Fixed 2026-08-15** — the backup now carries each prompt's source `orgId`; restore recreates it in that org when the user is still a member (dedup keyed per (org,name,type)), else falls back to the current org. Old backups without `orgId` keep today's behaviour. 2 tests (both-orgs-survive; orphaned-prompt-falls-back). |
| DATA-20 | Medium | `sendMail` failures are unhandled on the `PendingBundleAudience` path → the DB can record a notification/invite as delivered when it wasn't. | Recipients believe they were never invited; owners believe they were. Silent divergence. | **[Wave C]** Await the send, and only mark delivered on success; record failures for retry. | **Fixed 2026-08-15** — confirmed the only mail path is the bundle invite (notifications are in-app DB rows). The invite loop now collects failed sends and returns `failedInvites`/`emailsFailed` so the owner is told which invitations bounced (the pending row still stands, so the invite works on registration — honest feedback, not a rollback). Also wrapped `createNotifications` best-effort so an in-app notify failure can't 500 an already-committed publish. |
| DATA-21 | Medium | `shortCuid()` (`Math.random()` + `Date.now()`, in [`full-backup.ts`](../app/lib/full-backup.ts) and [`org-backup.ts`](../app/lib/org-backup.ts)) can collide mid-restore. | A PK collision aborts a restore, or worse mis-parents a row onto an existing id. Rare but catastrophic during disaster recovery. | **[Wave C]** Use `crypto.randomUUID()` (already used elsewhere) or the cuid library; never Math.random for identity. | **Fixed 2026-08-15** — `shortCuid()` is now `"c" + crypto.randomUUID().replace(/-/g,"")` in both files: 122-bit entropy, no shared-timestamp collision, same `c`-prefixed cuid-like shape. |
| DATA-24 | Low | Two independent connection pools (Prisma adapter + raw `pgPool` in [`db.ts`](../app/lib/db.ts)) can't share a transaction, so raw JSON writes fall outside a surrounding Prisma `$transaction`. | JSON-field writes (unavoidable under Prisma 7) aren't atomic with their model writes → partial state on failure. | **[Wave F]** Where atomicity matters, run the raw write via the Prisma tx client (`tx.$executeRawUnsafe`) rather than the standalone pool. Several paths already do this; audit the rest. | Not fixed |
| DATA-29 | Medium | Wipe-restore's data-loss COUNT guard runs **outside** the TRUNCATE transaction (TOCTOU). | A concurrent write between the count and the truncate defeats the guard → the very data-loss it's meant to prevent. | **[Wave C]** Move the guard COUNTs inside the same transaction as the TRUNCATE, or take an advisory lock for the restore. | **Fixed 2026-08-15** — the missing-table COUNT guard moved INSIDE the TRUNCATE transaction and now `LOCK TABLE … ACCESS EXCLUSIVE` on the checked tables before counting, so a concurrent write can't land rows between the count and the truncate. 1 test (guard refuses + live rows survive). |
| DATA-30 | Low | Per-table restore's `inserted` count excludes updates, and a malformed payload throws an unguarded `TypeError` → 500. | Confusing restore reports; an ugly 500 instead of a clear validation error on a bad file. | **[Wave D]** Count updates; validate payload shape up front and return a 400 with the problem. | **Fixed 2026-08-15** — summary counts rows RESTORED (ins+upd) not just inserts; a non-array/non-object payload value is skipped-with-log instead of TypeError; a missing `tables` object throws `BackupValidationError` → route returns 400. Test. |
| DATA-32 | Medium | *(New, 2026-08-15 — found reviewing the co-authoring concurrency model.)* The diagram save route ([`diagrams/[id]/route.ts`](../app/api/diagrams/[id]/route.ts)) applied a `version` compare-and-swap when a client sent its version, but a data save with **no** version was accepted as a silent unconditional write. | A legacy/rogue client — or any writer that skips the version — could silently clobber a concurrent editor's changes, defeating the CAS that protects everyone else. | **[Wave C]** Require optimistic concurrency on data saves: a version-less data write is rejected unless it explicitly opts into an authoritative overwrite (`unconditional:true`). | **Fixed 2026-08-15** — new pure `classifyDiagramWrite` (`app/lib/diagram/saveConcurrency.ts`): data+version → CAS; data, no version, no opt-in → **400 reject**; data+`unconditional:true` (or metadata-only) → unconditional write. The three authoritative tool writers (Simulator write-back, PCF create, initial-write-after-create) now pass `unconditional:true` on purpose; the main editor already sends its version. 5 tests. |

## Diagram engine (reducer / routing / geometry)

| Code | Sev | Nature of the issue | Risk if not fixed | Recommended fix | Status |
|---|---|---|---|---|---|
| ENG-05 | Medium | `REMOVE_SPACE` leaves corner elements un-shifted in cross (both-axis) removal zones. | Elements drift out of alignment after a space-removal in a corner region → visibly broken layout, silent. | **[Wave D]** Handle the both-axis case in the shift math (shift on the union of the two axis conditions). | **Fixed 2026-08-15** — dropped the blanket `partialOverlap → return` bail; each axis shifts independently (a strip-straddling element matches neither branch anyway), so a corner overlapping one strip still shifts on the clean axis. 3 tests. |
| ENG-06 | Medium | An interleaved second drag overwrites the pre-drag snapshot → the first action is lost from history. | Undo skips a change the user made — surprising, and it can lose work. | **[Wave D]** Snapshot on drag *start* (once), guard against re-snapshotting mid-gesture. | **Deferred 2026-08-15** — the group-move path is already guarded (`!groupDraggingRef`); the residual overwrite is subtle and needs interactive reproduction to fix safely. |
| ENG-07 | Medium | `offsetAlongFromPoint` / `getOffsetAlong` divide by element `width`/`height` with no zero-guard ([`routing.ts`](../app/lib/diagram/routing.ts)); a 0–1 clamp was added but the **divisor** is still unguarded → `NaN` survives when w/h is 0. | A zero-size element yields `NaN` offsets → connectors render to `NaN` coordinates (invisible / broken). | **[Wave D]** Guard the divisor: `w > 0 ? (pt.x-el.x)/w : 0.5`. Two call sites. | **Fixed 2026-08-15** — both `offsetAlongFromPoint` + `getOffsetAlong` guard the divisor (span > 0 ? … : 0.5), so a zero-size element can't produce a NaN that survives the clamp. |
| ENG-08 | Medium | Containment clamp can invert detour lines so the route crosses through the obstacle it was avoiding. | Connectors visibly cut through shapes on certain containment geometries. | **[Wave D]** Re-derive the detour after clamping and re-check obstacle intersection; fall back to the outer route. | **Deferred 2026-08-15** — the fix (re-derive + re-check clamped candidates vs obstacles) is non-trivial routing geometry needing visual reproduction; not safe to do blind. |
| ENG-09 | Low | `SWAP_LANES_VERTICAL` reorders the connectors array → silent draw-order (z-order) change. | Connectors change stacking after a lane swap — cosmetic but surprising. | **[Wave F]** Preserve connector order; swap only the lane geometry. | ✅ Fixed 2026-08-16 |
| ENG-10 | Low | `swapLane` bypasses the 100-entry history cap. | Unbounded history growth via repeated swaps → memory creep in long sessions. | **[Wave F]** Route the swap through the same capped `pushHistory` as other mutations. | ✅ Fixed 2026-08-16 |
| ENG-11 | Low | `INSERT_SPACE` pushes a history snapshot on **every mouse-move frame** of a shift-drag. | History floods with intermediate frames → undo becomes useless (one step per frame) + memory. | **[Wave F]** Snapshot once at gesture start; coalesce the drag into a single history entry. | ⏸ Deferred 2026-08-16 — gesture-lifecycle + Canvas wiring; needs interactive verify |
| ENG-12 | Low | `ancestorsOf` does a linear `find()` per hop → O(n²) obstacle setup per recompute. | Slow routing recompute on large diagrams (compounds CANVAS perf items). | **[Wave E]** Build an id→element Map once per recompute and walk parents through it. | ✅ Fixed 2026-08-15 |
| ENG-13 | Low | `consolidateWaypoints` returns its **input array by reference** for short paths → aliasing trap. | A later in-place edit mutates shared state → spooky-action bugs. | **[Wave F]** Always return a fresh array. | ✅ Fixed 2026-08-16 |
| ENG-14 | Medium | `updateLabelLive` mutates persisted label+geometry after an undo without invalidating the redo branch. | Redo after a live-label edit replays stale state → inconsistent document. | **[Wave D]** Invalidate the redo stack on the first live-label mutation, like other setters (ENG-03 pattern). | **Fixed 2026-08-15** — `updateLabelLive` now calls `invalidateRedo()` (idempotent) so a redo after typing over an undone state can't replay stale geometry. |
| ENG-15 | Low | `correctAllConnectors` rewrites persisted waypoints without `pushHistory` / `invalidateRedo`. | A correction pass silently makes an un-undoable change to saved geometry. | **[Wave F]** Wrap in the standard history push + redo-invalidate. | ✅ Fixed 2026-08-16 |
| ENG-16 | Low | Rectilinear waypoint-preservation uses a different obstacle set than the main pass → data-object flip-flop. | Connectors to/from data objects jitter between two routes on successive recomputes. | **[Wave F]** Use one obstacle set for both passes. | ⏸ Deferred 2026-08-16 — routing geometry; needs visual repro |
| ENG-17 | Low | `recomputeAllConnectors([conn])` rebuilds the full element Map per connector, per drag frame. | Redundant O(n) work per connector per frame → drag lag on big diagrams. | **[Wave E]** Build the Map once and pass it in. | ✅ Fixed 2026-08-16 |
| ENG-18 | Low | `ensureContainersEncloseChildren` recomputes ancestor depth inside the sort comparator → O(n² log n). | Slow container-enclose pass on deep hierarchies. | **[Wave E]** Precompute depth once into a map; sort on the memoised value. | ✅ Fixed 2026-08-15 |
| ENG-19 | Low | `getAllDescendantIds` is O(subtree×n) per column inside the vertical-swimlane drag frame. | Drag lag on wide swimlane diagrams. | **[Wave E]** Precompute a parent→children index once per gesture. | ✅ Fixed 2026-08-16 |

## Canvas & renderers (performance / leaks)

| Code | Sev | Nature of the issue | Risk if not fixed | Recommended fix | Status |
|---|---|---|---|---|---|
| CANVAS-02 | High | O(n²) connector-hump computation rebuilt every render (`indexOf` + slice/map per connector). | Pan/zoom stutters badly on large diagrams; render cost grows quadratically. | **[Wave E]** Memoise the hump map; compute crossings once per data change, not per render. | Not fixed |
| CANVAS-03 | High | `nonContainers` sort is O(n² log n): the comparator calls `elements.find()` in a parent-walk. | Same — every render pays a quadratic sort. | **[Wave E]** Precompute depth/parent into a map before sorting. | Not fixed |
| CANVAS-04 | High | Domain-diagram obstacle check is O(connectors×elements×waypoints), unmemoised, every render. | Severe lag on domain diagrams with many connectors. | **[Wave E]** Memoise per (elements, connectors) identity; short-circuit unchanged frames. | Not fixed |
| CANVAS-05 | High | `SymbolRenderer` / `ConnectorRenderer` are unmemoised and take fresh inline closures → full re-render every pan/zoom frame. | The single biggest pan/zoom cost: every shape re-renders on every frame. | **[Wave E]** `React.memo` the row renderers; hoist/`useCallback` the handlers so props are stable. | Not fixed |
| CANVAS-06 | Medium | Connector-drop highlight is O(n²) via `getElementPoolId` linear find per element. | Lag while dragging a connector end over a big diagram. | **[Wave E]** Precompute element→pool once per drag. | Not fixed |
| CANVAS-07 | Low | Pre-drag gesture listeners are never cleaned up on unmount. | Listener leak → memory growth and possible double-handling after navigation. | **[Wave F]** Return a cleanup from the effect that removes the listeners. | ⏸ Deferred 2026-08-16 — React lifecycle in Canvas.tsx; batch w/ CANVAS render work at desk |
| CANVAS-08 | Low | Connector-label focus-clear listener leaks if the component unmounts before the next mousedown. | Same class of leak. | **[Wave F]** Clean up on unmount. | ⏸ Deferred 2026-08-16 — React lifecycle in Canvas.tsx; batch w/ CANVAS render work at desk |
| CANVAS-09 | Low | Group-drag auto-scroll `setInterval` is cleared only on mouseup, not on unmount. | A drag interrupted by navigation leaves a running interval. | **[Wave F]** Clear the interval in an unmount cleanup as well. | ⏸ Deferred 2026-08-16 — React lifecycle in Canvas.tsx; batch w/ CANVAS render work at desk |

## UI / client data-loss

| Code | Sev | Nature of the issue | Risk if not fixed | Recommended fix | Status |
|---|---|---|---|---|---|
| UI-03 | Medium | Folder-tree changes are saved via a module-level 500 ms debounce with **no flush on navigation** ([`ProjectDetailClient.tsx`](../app/(dashboard)/dashboard/projects/[id]/ProjectDetailClient.tsx)). | A folder rename/move made and immediately navigated away from is lost. | **[Wave C]** Flush the pending debounce on route change / `beforeunload` (a `flushFolderTree()` helper already exists — call it on unmount). | **Fixed 2026-08-15** — a mount effect flushes the pending folder-tree write on SPA unmount (`flushFolderTreeSave`) and on page unload (`pagehide`/`beforeunload`) via a new keepalive fetch, so a folder move/rename made just before navigating away is no longer lost. |

## Import / export & interop

| Code | Sev | Nature of the issue | Risk if not fixed | Recommended fix | Status |
|---|---|---|---|---|---|
| IO-02 | High | DDL importer drops FK relationships when table-name casing differs between definition and reference. | Imported physical models silently lose relationships → wrong diagram. | **[Wave D]** Normalise case for table-name matching in FK resolution. | ✅ Fixed 2026-08-15 |
| IO-03 | Medium | Unbounded recursion on nested sub-processes / lane sets (no depth guard) in an importer. | A crafted (or pathological) file blows the stack → 500 / crash. | **[Wave D]** Add a depth cap that degrades gracefully (like the sim splice cap). | ✅ N/A — importer is iterative (no recursion depth); no stack risk 2026-08-15 |
| IO-04 | Medium | O(n²) element lookups via `ctx.elements.find` during BPMN flow/lane wiring. | Slow import of large BPMN files. | **[Wave E]** Build an id→element Map once. | ✅ Fixed 2026-08-15 |
| IO-05 | Medium | BPMN importer discards sequence-flow **condition expressions**, writing a literal `"true"` ([`importBpmnXml.ts`](../app/lib/diagram/bpmn/importBpmnXml.ts)). | Conditional routing is lost on import → simulation/semantics wrong for imported diagrams. (Adjacent to the new gateway work, which now round-trips `default` but not conditions.) | **[Wave D]** Preserve the condition into `branchCondition` instead of the placeholder. | ✅ Fixed 2026-08-15 |
| IO-06 | Medium | BPMN importer leaves a dangling `boundaryHostId` when the host has no `BPMNShape`. | A boundary event references a dropped host → render/logic gaps. | **[Wave D]** Null the `boundaryHostId` (or drop the event) when the host isn't materialised. | ✅ Fixed 2026-08-15 |
| IO-07 | Medium | Unsanitised diagram name interpolated into the `Content-Disposition` export header (Visio v2/v3/test-vsdx, e.g. [`visio-v2/route.ts`](../app/api/export/visio-v2/route.ts)). | Header injection / malformed downloads via a crafted diagram name (CR/LF, quotes). | **[Wave D]** Sanitise the filename (strip CR/LF + quotes) and use RFC 5987 `filename*=` encoding. | ✅ Fixed 2026-08-15 |
| IO-08 | Low | DDL parser writes attacker-controlled table names into a plain-object map key (proto-pollution surface). | A `__proto__`/`constructor` table name could pollute the prototype → unexpected behaviour. | **[Wave D]** Use a `Map`, or `Object.create(null)`, for the name→def index. | ✅ Fixed 2026-08-15 |
| IO-09 | Low | BPMN importer id minting uses `Math.random()` with no cross-mint collision guard. | Rare id collision within a single import → merged/broken elements. | **[Wave D]** Use `crypto.randomUUID()` (as the sim adopt path does) or track minted ids. | ✅ Fixed 2026-08-15 |
| IO-10 | Low | DDL enum detection is case-sensitive on the PK column `code` → enum tables imported as plain classes. | Enum tables mis-classified on differently-cased schemas. | **[Wave D]** Case-insensitive compare for the `code` heuristic. | ✅ Fixed 2026-08-15 |

## Config & platform

| Code | Sev | Nature of the issue | Risk if not fixed | Recommended fix | Status |
|---|---|---|---|---|---|
| CFG-02 | High | `AUTH_SECRET` is the literal placeholder in local `.env`, and there is **no code-side weak-secret guard**. | If the placeholder ever reaches prod, all sessions are forgeable (catastrophic). No tripwire catches it. | **[Wave B]** Fail startup if `AUTH_SECRET` is missing/short/equals the known placeholder. Confirm prod uses a real 32-byte value. | **Fixed 2026-08-14** — [`app/lib/env.ts`](../app/lib/env.ts) `checkEnv` rejects a missing / placeholder / <32-char `AUTH_SECRET`; run from [`instrumentation.ts`](../instrumentation.ts) at boot (Node runtime), throwing in **production** and warning-only in dev. ⚠ still confirm prod `AUTH_SECRET` is a real value (the guard now makes a placeholder a hard boot failure, so a bad deploy fails loudly). 12 tests. |
| CFG-03 | High | No security headers anywhere — no CSP, `X-Frame-Options`, HSTS, or `X-Content-Type-Options` ([`next.config.ts`](../next.config.ts) has no `headers()`). | Clickjacking, no XSS mitigation-in-depth, no transport pinning. Compounds the stored-XSS class (CANVAS-01 was one instance). | **[Wave B]** Add a `headers()` block: a baseline CSP, `X-Frame-Options: DENY` (or `frame-ancestors`), `X-Content-Type-Options: nosniff`, HSTS. Start report-only for CSP. | **Fixed 2026-08-14** — `headers()` added: `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, HSTS (1yr), and a **report-only** CSP (the app relies on inline styles/scripts today, so enforcing would break the UI — report-only surfaces violations without breaking; **follow-up: measure violations → nonce the inline surface → make it enforcing**). |
| CFG-04 | Medium | No env-var validation; security-critical secrets read via non-null assertion (`!`) only. | A missing secret surfaces as a confusing runtime crash deep in a request, not a clear boot failure. | **[Wave B]** Validate required env at boot (a small schema); fail fast with a clear message. | **Fixed 2026-08-14** — same [`env.ts`](../app/lib/env.ts) validates `DATABASE_URL` (error), `AUTH_TRUST_HOST` (warn), and a half-configured Entra pair (error) at boot. Individual `process.env.X!` reads remain but a missing value now fails fast at startup with a clear message. |
| CFG-05 | Medium | The `proxy` matcher covers `/dashboard`, `/diagram`, `/portal`, `/m` but **not** the `(dashboard)` route-group's own URLs; auth relies on each page calling `auth()`. | A new page under the group that forgets its own `auth()` is silently public. Fragile by construction. | **[Wave B]** Broaden the matcher to cover the group, or add a layout-level guard so auth isn't per-page opt-in. | **Fixed 2026-08-14** — a single `PROTECTED_PREFIXES` in [`auth.config.ts`](../auth.config.ts) now drives the `authorized` callback, and [`proxy.ts`](../proxy.ts) lists a matcher entry for every one (help, matrix, notifications, processes, tech-notes added). [`tests/config/route-protection.test.ts`](../tests/config/route-protection.test.ts) fails if the two ever drift. |
| CFG-06 | Medium | `/matrix` is publicly reachable despite a comment asserting route-group auth. | An unauthenticated visitor reaches the Matrix/Simulator shell surface. | **[Wave B]** Add `/matrix` to the matcher (or gate it), and reconcile the misleading comment. | **Fixed 2026-08-14** — `/matrix` is now in `PROTECTED_PREFIXES` + the matcher (via CFG-05), and the false "auth-gated by the route group" comment on the page was corrected. |
| CFG-07 | Low | `X-Powered-By: Next.js` not disabled. | Minor fingerprinting aid. | **[Wave B]** `poweredByHeader: false` in `next.config.ts`. | **Fixed 2026-08-14** — `poweredByHeader: false`. |
| CFG-01 | Low | Live secrets in local `.env` (gitignored/never committed). | Low while it stays local; a shared/backed-up copy would expose them. **Rotate** and keep off shared locations. | **[Wave F]** Rotate the values; confirm `.env` is never in a backup/share path. | Not fixed |
| CFG-08 | Low | Unused PGlite packages remain in production `dependencies` (`@electric-sql/pglite*` in [`package.json`](../package.json)). | Larger image, larger dependency attack surface for code that's dead. | **[Wave F]** Remove both packages; PGlite was retired 2026-04-12. | ✅ Fixed 2026-08-16 |
| CFG-09 | Low | Production auth depends on a pre-release beta of `next-auth` (`^5.0.0-beta.30`). | Beta churn / unpatched security fixes; upgrade risk deferred indefinitely. | **[Wave F]** Track the v5 stable release and plan a pinned upgrade. | Not fixed |
| CFG-10 | Low | Microsoft token refresh computes `NaN` expiry when `expires_in` is absent → refresh silently disabled. | SharePoint connections silently stop refreshing → intermittent auth failures. | **[Wave F]** Default a sane expiry when `expires_in` is missing; never store `NaN`. | ✅ Already mitigated — every expires_in site uses `?? 3600` fallback (added w/ SEC-05); no NaN possible 2026-08-16 |
| CFG-11 | Low | *(New, found doing CFG-05.)* `/test/*` scratch pages (`/test/db`, `/test/session`, `/test/api`) build into the production bundle and are **not** behind the auth matcher. | If they echo DB/session state, an unauthenticated visitor could read internals. Needs confirmation of what each page renders. | **[Wave F]** Confirm what they expose; delete them from the production build (or gate + guard them). | Not fixed (needs confirmation) |

## Meta

| Code | Sev | Nature of the issue | Risk if not fixed | Recommended fix | Status |
|---|---|---|---|---|---|
| TEST-01 | Medium | The **test suite itself** was explicitly excluded from the baseline audit and has never been reviewed (the memory note flags ~31 `orgContext` mocks as "odd" and not to be trusted as a safety net). | Green tests may be giving false assurance — mocked auth/tenancy could hide the very access-control bugs above. | **[Wave F]** A dedicated pass over `tests/`: audit the shared mocks (especially `orgContext`), confirm the auth/tenancy tests actually exercise the guard, and remove or fix misleading fixtures. | Not fixed |

---

## Notes for whoever works this

- **Verify-in-prod-mode items** from the baseline are done but unconfirmed: the Stripe fixes (DATA-04/17/31) still want a Stripe **test-mode** run, and the per-table restore (DATA-27/28) wants a sandbox re-test. They're marked Fixed in the baseline doc; re-confirm before relying on them.
- **`Needs confirmation`** here means the finding is plausible from reading the code but wasn't reproduced this pass — reproduce before fixing so the fix is targeted.
- Keep audit evidence **redacted**: a prior finder quoted a real Azure secret into its output and GitHub push-protection blocked the commit. Don't paste credential-like strings into this file.
- When an item is fixed, set Status to `Fixed (<short-sha>)` and, if it's a security item, add a one-line note in the baseline `Source-Code-Audit.md` summary table so the two stay reconciled.
